<!-- SPDX-FileCopyrightText: 2026 True Positive LLC -->
<!-- SPDX-License-Identifier: GPL-3.0-only -->

# Status & TODO

The canonical status + pending-work list. Architecture and the spec/plan map live in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md); the full decision log (incl. per-spec "out of scope") in [docs/brainstorming-decisions.md](docs/brainstorming-decisions.md).

**Headline:** design complete; a connect-and-get-a-shell MVP builds for the iOS Simulator. The protocol + logic tiers are built and Linux-tested; the app shell is built and validated by macOS CI. Not yet device-installable (needs Apple Developer signing).

**Tests green:** 17 Rust unit + 35 Rust integration (vs containerized `sshd`) + 833 Swift (SemicolynKit + SeedKit), all on the Linux fast loop.

## Phase status

| Phase | Scope | State |
|---|---|---|
| **0 — Foundations** | Cargo + SwiftPM workspace, UniFFI bridge proof, design tokens, data model, AES-256-GCM record envelope | ✅ Done |
| **1 — SSH core** | russh behind UniFFI: handshake, host-key TOFU, auth (password / publickey / keyboard-interactive), OpenSSH cert auth, PTY shell, local/remote/dynamic forwards, ProxyJump, 4-tier algorithm allowlist | ✅ Done¹ |
| **2a — Storage core** | Host/identity schema + resolution table; `BlobStore`→`EncryptedRecordStore`, `SecretStore`, `HostKeyStore`; repository invariants; sync taxonomy | ✅ Done |
| **2b-i — Key minting** | ed25519 generation + OpenSSH-key import in the Rust core; `IdentityService` (mint→Keychain→metadata); `CoreIdentityMinter` bridge; publickey connect from a stored identity | ✅ Done (iCloud-Keychain flavor) |
| **2b-ii — Sync + SE** | CloudKit Private DB + sync engine; Secure-Enclave flavor (`SecAccessControl` + russh→SE signing bridge) | ⏳ enrollment-gated |
| **3 — Terminal + tmux** | Control-mode stack done²; **Plans A+B+C+D done**: probe→attach `tmux -CC`, native pane layout, multi-window, debounced resize, raw-PTY degrade; terminal UX (bell, OSC 52, titles, URL tap, cursor, mouse dot, pinch-zoom); per-pane context detection (engine + `pane_current_command` poll + observable) + mid-session crash banner. | ✅ Done³ |
| **4 — Keybar, input & predictor** | **4a + 4b + 4c done**: 4a mount + core input slots + keystroke codec + Ctrl-lock modifier SM + input router; 4b context promotions (bronze from `paneContexts`) + Fn mode (F1–F12, Fn SM, htop/top/mc auto-engage) + per-pane DECCKM; 4c predictor strip (input token tracker → engine suggestions → auto-hiding chip row, tap-to-complete, learn/harvest/flush, incognito-gated) — all compile-validated on macОS CI (interaction/visual unverified pending a Simulator/device). **4d-1 done**: Codable keybar layout + sticky-rule mutations (reorder/remove/move-across-divider) + reverse-bar + persisted `KeybarSettingsStore` + Settings→Keybar list editor (Esc-pill long-press entry); pure core Linux-tested, editor macОS-CI-only. **4d-2 done** (PR #18): macro model + `{Ctrl+R}…{Enter}` template parser + custom-slot binding model (4 gestures, ≥1-binding rule) + `KeybarLibrary` folded into settings (back-compat decode) + macro→bytes expansion / `fireMacro` + `MacroRecorder`; App tier: searchable Launcher, custom-slot editor, template/record macro creation, wired "+ Add" sheet (core Linux-tested, App macОS-CI-only). Placeholders deferred to v2. **4e done** (PR #19): hardware-keyboard Cmd-shortcut catalog (15 actions / split + ⌘1–9 aliases) + resolver + ⌘-HUD titles; `compactKeybarSlots` + hide-with-hardware-keyboard setting; `TmuxCommand.killWindow`/`selectPaneRelative`; App tier: `perform` dispatch (window/pane/split/clear/paste/reconnect + launcher/settings/tips/new-connection sheets), GameController detection → compact/hidden keybar, `.keyboardShortcut` overlay, Tips screen, Settings toggle (core Linux-tested, App macОS-CI-only). Gaps: ⌘C native to SwiftTerm, ⌘F find deferred. | ✅ Done⁴ |
| **MVP app shell** | iOS app target + SwiftTerm wired via UniFFI: connect → password/keyboard-interactive/**publickey** auth → shell, real host-key TOFU trust | ✅ Builds for Simulator |
| **5–7 — UI & ship** | Host CRUD UI + identity create/import + connect-from-saved done. Standalone Identities & Keys mgmt, connection-management UI, settings, IAP, App Store polish pending | ◐ Host CRUD done, rest pending |

¹ The `ssh-ed25519-cert-v01@openssh.com` **host**-certificate variant is deferred — blocked on russh 0.61 (verifies the server host key only as a plain `PublicKey`; no CA/principal/validity path). A guard test prevents advertising it until upstream support lands.
² Control-mode stack verified against real `tmux` in the `sshd` fixture, incl. the DCS-wrapped live `-CC` stream.
³ Plan D ships the context **engine + signal + observable** (`PaneContextMachine` dwell SM, `PaneContextStore`, `PromotionRegistry`/catalog, `list-panes` poll, `@Published paneContexts`); the keybar **visual** consumption (promoted slots, engage/disengage animation, per-pane pin, kill-switch) is Phase 4. Mirrors "predictor engine done / UI pending".
⁴ Phase 4 is feature-complete and CI-validated, but the whole keybar surface (4a–4e) is **interaction-unverified** pending a Simulator/device pass — see "Next". 4e device note: SwiftUI `.keyboardShortcut` resolves by character, so the shifted `⌘|` / `⌘?` bindings want a real-hardware check.

## Next (unblocked dev work)

- **Mosh transport (real interop)** — **M1 + M2 + M3 + M4 SHIPPED + merged to `main`** (2026-07-02/03); Mosh is wired into the live connect flow with pre-first-frame SSH fallback. **Only the device/Simulator + TestFlight manual pass remains.** Specs/plans: `docs/superpowers/specs/{2026-07-02-mosh-transport-design,2026-07-03-mosh-m3-m4-bridge-wiring-design}.md` + plans `{2026-07-02-mosh-transport,2026-07-03-mosh-m3-m4-bridge-wiring}.md`.
  - **M2 (merged `7e8cb95`, PR #31-era):** pure Linux-tested Kit units — `parseMoshConnect` (MOSH CONNECT parser), `moshServerCommand` (bootstrap argv), `moshLaunchDecision` (mosh-vs-SSH) — plus a Rust `open_exec → mosh-server new → MOSH CONNECT` interop test against a real mosh-server (Alpine sshd fixture gained `mosh`).
  - **M1 (merged `18ba374`, PR #36):** `blinksh/mosh` vendored as a pinned submodule (`extern/mosh` @ `3640d366`) → `scripts/build-mosh-xcframework.sh` cross-compiles **protobuf 3.21.12 + ncurses 6.5 + Mosh** (crypto = Apple CommonCrypto, so **no OpenSSL/Nettle**) into `Mosh.xcframework` (device arm64 + arm64/x86_64 sim); linked as a `#if os(macOS)` binaryTarget in `Package.swift` and built by macOS CI. **CI green:** framework builds/links + exports `mosh_main`; app `BUILD SUCCEEDED`. Took 14 CI iterations resurrecting the archived Blink iOS build for Xcode 16.2 / iOS-18 SDK.
  - **M3 (SHIPPED, PR #37):** `MoshSession` Obj-C++ bridge + `ConnectionViewModel` Mosh branch (bootstrap → `moshBranchOutcome` → `MoshSession` → SwiftTerm I/O), pre-handoff SSH fallback + `MoshFallbackBanner`. Bridge drives the real `mosh_main` over a pipe pair (unbuffered `fout`, mosh + reader threads, `pthread_kill(SIGWINCH)` resize) with a single-mutex/join-before-close teardown; a macOS-CI `SemicolynBridgeTests` target drives it against a fake-`mosh_main` loopback. Also fixed M1's `Mosh.xcframework` to ship a **self-contained** archive (object-explode merge, `libtool` basename-dedup fix) + link `libz`. ~11 CI iterations.
  - **M4 (SHIPPED, PR #38 → main `4cdb9a1`):** pre-first-frame SSH fallback — `MoshSession` gains `onFirstFrame` (fires exactly once on the first output byte = UDP handshake completed, gated under `_lock`, cleared by `-stop`); `ConnectionViewModel` tracks `moshFirstFrameSeen` so `onEnd` **before** a frame (UDP blocked / crypto mismatch) falls back to SSH on the **same retained connection** with a *"Mosh UDP unreachable"* banner (instead of dead-ending to `.failed`), while `onEnd` **after** a frame reuses the mid-session crash banner. Shared tmux/raw connect tail extracted into `attachSSHShell(conn:host:defaults:)`. Also verified: no `scenePhase` transition calls `stop()`/`teardown()`, so Mosh's intrinsic roaming/background-foreground is never interfered with (Mosh reconverges on its own; a true socket kill → `onEnd` → crash banner is the accepted v1). macOS-CI bridge test asserts `onFirstFrame` fires exactly once across all output bytes.
  - **NEXT — M4 manual/device:** roaming Wi-Fi↔cellular + background/foreground device feel pass; stretch macOS real-server UDP interop test (Task 8). Do the manual Simulator connect + **TestFlight build** (first point Mosh is user-testable) — Tasks 6–8 of `2026-07-03-mosh-m3-m4-bridge-wiring.md`.
- **On-device testing via TestFlight** — CI lane prepped + dormant (`.github/workflows/release-testflight.yml`, gated on repo var `TESTFLIGHT_ENABLED`). No personal Mac needed: the GitHub macOS runner archives/signs/uploads via an App Store Connect API key. Enable once the paid Dev Program enrollment completes — one-time steps + secrets in `docs/testflight-setup.md`. Unblocks every owed device feel-pass below.
- **App icon — SHIPPED** (2026-06-30). Mark = a shell-prompt chevron `❯` fused with a comma ("prompt + semicolon"): coral prompt `#FF6F5E` + off-white "text" comma `#CFD6E4` on midnight `#12151F`, flat. Vector master `assets/semicolon-mark.svg` (two paths, theme/glow-able); 1024² opaque PNG wired into `App/Assets.xcassets/AppIcon.appiconset/icon.png` + `Contents.json` filename. Re-theme/re-size any time via `scripts/logo_render.py` (recolor + uniform-glow + multi-size pipeline). The interactive color playground + final renders live in `mockups/drafts/` (+ `logo-renders/`). Follow-ups: per-shape **neon-glow** variants are for marketing/site, not the icon; **alternate/Pro app icons** still tracked under the StoreKit slice.
- **Cursor-placement Simulator feel-pass** (needs a Mac; App-tier compile-validated only) — the gesture is built but UNTUNED. Verify/tune on a Simulator/device: halo rides the cursor on the focused pane (and its refresh cadence during fast output); drag moves the cursor in readline/vim incl. under `mouse=a`; **gain curve** (1:1 ≤600pt/s → 3× cap) + **1.5-cell vertical dead-zone** feel; **gesture coexistence** — the halo-gated pan must NOT block scroll / window-switch / long-press-select outside the ~60pt halo; engage/lift haptics. Then build the deferred slices once their SwiftTerm APIs are confirmed live: **loupe magnifier** (read row chars via `getChar`, render with `LoupeWindow`), **offscreen `⌖` indicator** (`isOffscreen` + scroll-to-live), **selection-handle suppression** (set `CursorDragController.suppressed` from SwiftTerm's selection-active state). Tunables live in `CursorDragEngine` statics + `CursorDragController.haloRadius`.

- **Phase 4 — keybar, input & predictor: DONE** (4a #14, 4b #15, 4c #16, 4d-1 #17, 4d-2 #18, 4e #19). Follow-ups carried forward (deferred, noted in code): **4d-2** — parameterized macro placeholders (`{{host}}`, defaults, per-host remembered values, the v2 placeholder system); editing an existing macro's body from the Launcher; live-keybar capture for record mode (v1 uses a dedicated input pad). **4e** — ⌘C copy left to SwiftTerm's native path; ⌘F find-in-scrollback **dropped** (PR #25): tmux copy-mode search covers the persistent-session workflow and SwiftTerm ships no find API, so the dead shortcut was removed from the catalog (re-entry pointers in the external-keyboard spec's deferred section). Specs: `2026-06-15-keybar-customization-design.md`, `2026-06-17-external-keyboard-design.md`.
- **Simulator/device pass on the keybar (4a–4e)** — compile/CI-validated only; interaction + visuals unverified. Verify: mount-above-keyboard (`inputAccessoryView` vs the v1 `safeAreaInset`), slot recolor, gestures, predictor-strip slide/chips/tap-complete; **plus the unrendered spec visuals** — promoted-slot bronze top-edge accent (context spec §"Promoted slot visual") and Fn-locked brighter glyph + 4pt lock-dot (function-keys spec §"Visual"). **4d-1 editor**: Esc-pill long-press → Settings→Keybar; verify drag-reorder, swipe/edit-mode delete (Esc/Pad locked), per-row move-across-divider menu, reverse-bar mirror (`layoutDirection` flip), Reset — note 4d-1 used two sections + a per-row move action instead of the spec's single draggable divider (SwiftUI cross-section drag is unreliable), revisit. **4d-2 surfaces**: Launcher (search/pin/delete), custom-slot editor (4 binding rows, ≥1-binding Save gate), macro creation (template live-parse + record chip list), pinned-macro / custom-slot rendering (hint glyphs, `fireMacro`), "+ Add" sheet. **4e surfaces**: hardware-keyboard → compact/hidden keybar (GameController), the ⌘-HUD + each Cmd-shortcut firing (esp. shifted `⌘|`/`⌘?`), held-modifier passthrough, Tips screen, the hide-keybar Settings toggle. Gated on Apple enrollment for device.
- **Pro / StoreKit slice** (deferred from the theme picker) — the real one-time purchase: StoreKit purchase + restore + Family Sharing, the Supporter badge in About & Help, and alternate app icons. The theme picker shipped against a stub `ProStore` entitlement seam; this slice swaps the backing in behind that same surface. Spec: `2026-06-16-pro-paid-scope-design.md`. Partly enrollment-gated (App Store Connect product config).
- **Theme-picker Simulator pass** (App-tier, CI-validated only) — verify on the Simulator: Settings gear → Appearance → Theme renders both rows with swatches; Bell Bronze shows ✦Pro and routes to the upgrade placeholder without changing selection when Pro is off; debug-unlock then selecting Bell Bronze recolors chrome live; selection persists across relaunch. **SwiftTerm live-recolor:** WIRED 2026-07-01 (PR #31) — `applyPalette` re-installs the terminal's 16 ANSI colors + fg/bg/cursor/selection on theme change via `updateUIView`; the Simulator pass now just visually confirms the terminal *content* repaints on switch (chrome already recolors via `@Environment`). Minor cleanup carried: `HostListView` uses two `.navigationBarLeading` `ToolbarItem`s — fold into a `ToolbarItemGroup`.
- **Phase 3c deferred seams** — `onSSHLink` connect-prefill and active-pane title keying **SHIPPED** 2026-06-28 (PR #26, `8159455`): ssh:// link tap → `QuickConnectSheet` confirm → nested session (pure `parseSSHURL`); tmux window title now keyed to the active pane (pure `titleToPublish`/`titleOnActiveChange`). **Halo-suspend resolved** by the cursor-placement feature (PRs #27/#28): the halo now exists, mouse-mode deliberately does NOT suspend it (we synthesize arrows, not mouse events), and both `TODO(phase4)` markers are gone; the residual selection-handle suppression is tracked under the cursor-placement Simulator pass above. _3c Simulator pass (needs a Mac) still owes: confirm the ssh:// confirm-sheet → fullScreenCover transition doesn't flicker, and the title follows the active pane on `⌘]`._

## Enrollment-gated (Apple Developer Program org enrollment, in flight)

- **2b-ii** — CloudKit Private DB sync engine + Secure-Enclave identity flavor.
- **On-device / TestFlight testing** and code-signing — needs the signing identity.

## Deferred / when-needed

- **Accessibility review** — app-wide a11y pass (VoiceOver for terminal + keybar, Dynamic Type vs fixed cell font, low-opacity overlay + focus-border contrast, Reduce Motion for bell pulse / cursor blink, haptic opt-out, tap-target minimums). Best once terminal UX + keybar/Settings UI exist.
- **Nerd Fonts** — patched glyph fonts (powerline/dev icons) in the terminal renderer, if users hit missing glyphs. Ties into a Phase-4 Terminal Settings font picker.
- **russh host-cert gap** — see footnote ¹; blocked upstream.

## Resolved (recent)

- **Blue themes (Piece 2)** — shipped (Kit tier) 2026-07-02: **Neon Cobalt** (cool-electric, accent `#5A6EFF`) + **Glacier** (cool-soft pastel on a lighter slate, accent `#8AA6E8`), both Pro-gated (`Theme.catalog` entries 3 & 4). 849 Kit tests green; App surfaces them via the catalog (macOS-CI-validated). **Reframe from the original plan:** themes are distinguished by a full-palette *personality* (temperature × intensity), not just the accent hue — the "even-spaced azure / Azure Prism" idea was dropped. **Theme format decided:** a theme = a standard **iTerm2 `.itermcolors`** palette (16 ANSI + fg/bg) + one `accentSlot` + optional chrome overrides; surfaces/text/highlight are **derived** (surfaces = bg tint-steps, highlight = lighten(accent), status = red/green/yellow slots), Bell Bronze the one theme keeping hand-authored chrome. Pipeline `mockups/drafts/themes/build.py` parses iTerm2/base16 → derives → emits the (generated) gallery config + `Theme.fromANSI` Swift (`--swift`); this also seeds _Piece 3_ (import). Mockups: `mockups/drafts/2026-07-01-theme-gallery.html` (8-tab, incl. Dracula/Nord/Gruvbox/Catppuccin benchmarks) + `-derive-compare.html`. **Pending:** the warm-charcoal Bronze + derived-Midnight retune (gallery-only so far; would change 2 shipped themes).
- **Themed terminal palette (ANSI-16 infra)** — shipped 2026-07-01 (PR #31, `014a205`); **Piece 1** of a 3-piece blue-themes roadmap. `Theme` now carries an authored 16-color ANSI palette as the single source of hue, with **strict typed derivation** (`ANSISlot` / `ANSIPalette` / `ANSIRoleMap` → `Theme.fromANSI`: `accent.primary` + the 4 state colors are typed slot refs; `accent.highlight` authored; bell/focus/keybar/predictor/banner derived) and extended terminal tokens (`cursor`/`cursorText`/`selection`); a pure `TerminalPalette` DTO. Neon Midnight + Bell Bronze migrated through the factory with exact colors preserved (regression-guarded — coral `#FF6F5E` ≠ crimson `#E5455E`). App-tier `TerminalPaletteBridge` (`applyPalette` → SwiftTerm `installColors` + native fg/bg/caret/selection) finally applies theme colors to the **live** terminal — previously `terminal.bg/fg` reached only the picker preview — hooked in `TerminalScreen` + tmux panes, recoloring on theme switch. Kit Linux-tested (833 green); App macOS-CI-validated. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-01-themed-ansi-palette-infra*`. **Follow-ons:** _Piece 2_ (the two blue themes) SHIPPED — see the entry above; _Piece 3_ — theme import (iTerm2/base16 → `ANSIPalette`), largely seeded by the `themes/build.py` pipeline. Mockups: `mockups/drafts/2026-07-01-theme-*.html`.
- **Cursor-placement drag gesture** — shipped 2026-06-29 (PR #27 `db6bf53` pure engines + PR #28 `7e5de1f` App layer). Locked design `docs/brainstorming-decisions.md` §"Cursor placement". Pure (Linux-tested, 32 tests): `CursorDragEngine` (gain curve + 1.5-cell vertical dead-zone + sub-cell remainder), `CursorHaloGeometry`, `CursorArrowStream`, `LoupeWindow`. App (macOS-CI-compiled): `CursorHaloView` + `CursorDragController` — faint 60pt halo at the cursor + a halo-gated single-finger pan that synthesizes arrow keys (`encodeKey(.arrow)` → `sendTerminalInput`, reading the live cursor via the confirmed-public `getTerminal().buffer.x/.y`); installed raw-PTY (single) + tmux (per-pane, focused-only); works under `mouse=a`. **App layer is compile-verified only — UNTUNED** (see Next). **Deferred slices** (uncertain SwiftTerm APIs): loupe magnifier (needs `getChar` row text), offscreen `⌖` scrollback indicator (undocumented `scrolled(position:)`), selection-handle suppression (no public selection-active signal).
- **Predictor 4c follow-ups** — shipped 2026-06-27 (PR #22, 53c198f): `flushPredictor()` flushes learned state on app-background (`scenePhase`) as well as teardown; `TerminalShellOutput` gained an `onHarvestBytes` slot so raw-shell (degraded) output trains the predictor without the render closure clobbering it; tmux harvest restricted to visible panes (early-return on dropped panes); `output.onBytes`/`onHarvestBytes` nilled in `teardown`. App-tier, macOS-CI-validated; interaction-level check rides the pending Phase 4 Simulator/device pass.
- **Theme picker** — Settings → Appearance theme picker shipped (gear on the host list → Settings → Appearance → Theme). Neon Midnight (free) / Bell Bronze (Pro-gated). Pure catalog + Pro-gate (`ThemeID`/`ThemeDescriptor`/`Theme.catalog`/`resolveTheme`) is Linux-tested; live switching via root `\.theme` injection; Bell Bronze gated behind a **stub `ProStore`** seam (real StoreKit + Supporter badge + alt icons deferred — see Next). Spec/plan: `docs/superpowers/specs/2026-06-27-theme-picker-design.md`, `docs/superpowers/plans/2026-06-27-theme-picker.md`. Simulator pass + SwiftTerm live-recolor check pending (see Next).
- **Naming/trademark** — naming journey: **Glymr → neotilde → semicolyn**. Glymr collided with a LIVE registered GLYMR mark (2026-06-24) → forced to `neotilde`, which was ownable but disliked; a fresh round explored `fosfyre` (rename executed on the parked branch `refactor/rename-fosfyre`, never merged) before landing on **`semicolyn`** — a respell of *semicolon* (`;`, the shell command separator), owning the full namespace and trademark-clear. **Rename to semicolyn executed 2026-06-28** (this branch). `lumenhir` was the backup. SEMICOLYN mark not yet filed (pre-launch — cheapest moment to still change). Records: `docs/2026-06-28-naming-decision-semicolyn.md`; historical: `docs/2026-06-24-naming-decision-neotilde.md`, `docs/2026-06-28-naming-decision-fosfyre.md`; agent memory `naming-and-trademark`.
- **Default theme** — **Neon Midnight** (coral neon on midnight blue-black, bell-only glow); Bell-bronze retained as a switchable alternate. Spec: `docs/superpowers/specs/2026-06-25-neon-midnight-theme-design.md`.
