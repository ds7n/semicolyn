<!-- SPDX-FileCopyrightText: 2026 True Positive LLC -->
<!-- SPDX-License-Identifier: GPL-3.0-only -->

# Status & TODO

## Resume here (2026-08-05, end of session, context cleared)

**STATE: ET crash + spin + malformed-credential + "ET"-label all FIXED and DEVICE-CONFIRMED (build off PR [#120](https://github.com/ds7n/semicolyn/pull/120), CI-green run `31019077723`). ET connects + typing works. PR #120 is NOT merged (chose hold-until-device-confirmed; now confirmed = MERGEABLE). #119 superseded by #120.**

**EXACT NEXT ACTIONS (in order):**
1. **Merge PR #120** to `main` (lands the parse fix + UAF crash fix + 15s watchdog + Eternal Terminal label; both slices bundled). Squash-merge per convention. It's on branch `fix/et-callback-uaf-watchdog` (contains BOTH the parse-fix commits and the crash-fix commits, forked from the un-merged parse-fix, so main is 13 commits behind).
2. **THEN fix the `onEnd` clean-exit bug** (device-found this session, own slice): a clean `exit` shows a `.failed` error banner AND the terminal keeps accepting input, instead of gracefully ending. Root cause: ET `onEnd` in `attachET` (`App/ConnectionViewModel.swift`) sets `state = .failed(etFailureMessage(.handshakeFailed))` for EVERY end. FIX = mirror Mosh: a pure `etExitDecision` (Linux-tested, in `Sources/SemicolynKit/ET/`) distinguishing clean-end (onFirstFrame was seen -> graceful dismiss to connection list, NOT `.failed`) from pre-first-frame failure (-> the error path). Also: terminal must stop accepting input on end. See memory `et-onend-clean-exit-bug-2026-08-05` + `Sources/SemicolynKit/Mosh/MoshExitDecision.swift` + the Mosh onEnd (~ConnectionViewModel.swift:800-830). Keep the watchdog/resolve-once/ctx-release logic (correct); only change WHICH state onEnd sets.
3. Then: on-connect-command feature (memory `on-connect-command-todo-2026-08-05`), ET-over-tmux-`-CC` (native panes), predictor prose-vocab.

Prior context: 1a+1b merged; Transport picker merged (`87edea3`); Selection UI merged (PR #114 `0d80804`). Dev-host ET server is UP (sshd :22 + etserver v7.0.0 :2022). Syslog sink diagnostics: read via `docker exec <syslog container> sh -c "grep ... /var/log/semicolyn/semicolyn.log"` (host file root-owned, sudo blocked in-session).

**Device round 2 (build 110/112) surfaced TWO bugs, both fixed in #120:**
1. **"Malformed credential" (parse):** `parseETIDPASSKEY` demanded the WHOLE IDPASSKEY line be exactly `<16>/<32>`; a trailing CR (SSH PTY) tripped it. Fixed to upstream's fixed 49-char window. Plus an opt-in `transport` LogCategory (default off) logging the MASKED bootstrap payload (credential hidden, trailing junk visible).
2. **CRASH + infinite spin:** the device crash report (EXC_BAD_ACCESS at `ETSession.mm:62`) root-caused to a use-after-free: the 3 ET callback trampolines passed `(__bridge void *)self` UNRETAINED to `et_connect`, so a callback during a failed/blocked connect messaged freed memory. Fixed by `CFBridgingRetain`ing the ctx for the connection lifetime (mirrors `MoshSession.mm`), released exactly once after `et_close` joins the thread (opus-verified across all orderings). Also added a 15s connect watchdog (mirror `moshWatchdog`, resolve-once guard) so a blocked port-2022 handshake fails to a clean timeout instead of spinning. Picker now says "Eternal Terminal" (was "ET"). Two review-caught spec defects fixed in-loop (a leak the retain introduced; onEnd skipping cleanup).

**Eternal Terminal on-device retest (build off #120):** set a host Transport = Eternal Terminal, connect to the dev box (sshd :22 + etserver v7.0.0 :2022 up). EXPECT: it CONNECTS (parse fix) and does NOT crash (UAF fix); if port 2022 is blocked or the protocol mismatches, a clean "Eternal Terminal timed out: no response on port 2022 ..." after 15s (NOT a spin, NOT a crash). WATCH-ITEM: protocol-6-client vs 7.0.0-server, a mismatch now surfaces as a `.failed` (via watchdog/onEnd), not a hang; if that's what happens, the next fix is re-pinning the vendored ET client or a matching server. To diagnose any transport failure: flip Settings > Diagnostics > `transport` ON, reproduce, read the masked payload from the syslog sink (docker service: `docker exec <syslog container> sh -c "grep ... /var/log/semicolyn/semicolyn.log"`; host file is root-owned + this session's sudo is blocked). AFTER a green device test: merge #120 (lands both slices).

**Selection UI + terminal/keybar geometry: MERGED to `main`. PR #114 squash-merged, CI green, device-confirmed.**

- **PR:** [#114](https://github.com/ds7n/semicolyn/pull/114) `feat: custom terminal selection UI + keybar/tap geometry fixes`, **MERGED as squash `0d80804`** (branch deleted), all CI jobs green (macos/linux-swift/linux-rust/lint). **#113 CLOSED as superseded.** Full Kit suite 1387/0. Pre-merge reviewed by two fresh-context reviewers (Kit logic+tests / App wiring): 0 Critical, 0 Important; the one real Minor (a hidden `SelectionLoupeView` leaking into the ContainerView on pane teardown) was fixed in-branch (`5bb935e`, `detach()` now `removeFromSuperview` + nils the loupe) and re-verified macOS-CI green before merge.
- **Device-confirmed on TestFlight** (4 rounds): visible highlight on alt-screen, sub-word double-tap (full word), draggable handles, loupe offset-above-finger, reverse-video colors, last character included, keybar gap gone on first-connect AND post-app-switch.
- **What shipped:** the whole Topic 3 selection UI (yDisp absolute-row highlight fix, `subWordBounds`, focus-on-select, bracketed paste, handles, custom loupe, copy-menu re-summon, reverse-video colors, exclusive-end last-column fix) + geometry (tap-location via `caretFrame` true cell size; keybar overlap fixed by laying panes to `keyboardLayoutGuide` top). Design/plans: `docs/superpowers/specs|plans/2026-08-0{1,2}-*`.

**EXACT NEXT ACTION (2026-08-05):** ET slices **1a (`8cfd630`, PR #116) + 1b (`0f1b422`, PR #117) MERGED** to `main`. The **Transport picker + ET-interactive slice IS CI-GREEN, PR [#118](https://github.com/ds7n/semicolyn/pull/118) OPEN** (branch `feat/transport-picker`, all 4 jobs green run `30961990046`, full Kit suite 1439/0; TestFlight build going up, run `30964778764`). Built subagent-driven from spec/plan `docs/superpowers/specs|plans/2026-08-04-transport-picker*`; final opus review clean (0 must-fix). This slice made ET reachable (explicit per-host SSH/Mosh/ET picker replacing "Mosh silently wins") + interactive (typing + resize wired).

**THE NEXT STEP IS AN ON-DEVICE TEST (not more code):** once TestFlight build is "Ready to Test":
1. On the phone, edit a host whose `hostName` points at the dev box (sshd on :22 + etserver v7.0.0 on :2022 are UP and reachable), set **Transport = ET**, connect.
2. VERIFY: (a) ET connects (handshake succeeds), output renders; (b) you can TYPE (keystrokes reach the remote shell); (c) rotate / show-hide keyboard reflows the terminal (resize wired); (d) a host pointed at a port with no etserver shows the "Eternal Terminal could not connect: ..." `.failed` message (NOT a silent SSH fallback, NOT a hang).
3. **WATCH-ITEM at the handshake:** vendored client is `et-v6.0.2-374-gdfc75d663` (PROTOCOL_VERSION 6); server is marketing 7.0.0. Protocol (not marketing) version must match; expected compatible, but `MISMATCHED_PROTOCOL` is the risk. If it fires, re-pin the vendored ET client or confirm the server's protocol version.

After a green device test: merge #118, then the ET follow-ups are (i) §4 dedicated Retry/Cancel error screen (currently reuses the generic `.failed` banner, adequate for testing), (ii) extract the inline Transport picker binding into `InheritedBinding.swift` Kit helpers + tests (deferred minor), (iii) ET roaming banner. Three queued follow-ups remain:
1. **ET (Eternal Terminal) transport (user-requested 2026-08-03, "sooner rather than later"; ET is the user's PRIMARY targeted mode).** Track 3 = the only path to native `tmux -CC` panes **+** roaming together (Mosh structurally can't do `-CC`). Progress:
   - **✅ Portable core DONE:** `eternaltermlib` (`ds7n/eternaltermlib`, Apache-2.0, C ABI, CI-green, security-reviewed). Now @ `f3d544a` (added `ET_BUILD_TESTS` gate).
   - **✅ iOS xcframework build SHIPPED 2026-08-03 (PR #115, squash `ff0e674`, macos CI green).** `ETerminal.xcframework` cross-compiles + the iOS app links it. Submodules now tracked on main: `extern/{eternaltermlib, swift-sodium, ios-cmake}`. `scripts/build-etios-xcframework.sh` + `docs/et-ios-build-notes.md`. Corrected 3 plan decisions (SODIUM_LIB override, 3-thin-slices+lipo, leetal 4.5.0 OS/SIMULATOR), see memory `et-ios-build-vendored`.
   - **✅ `libetios` wrapper (slice 1a) MERGED to `main` (squash `8cfd630`, PR #116).** `ETSession` (`App/ET/ETSession.{h,mm}`, Obj-C++) over the `et_*` C ABI: private serial queue owns send/resize/close ordering (isClosed guard, never races per the ABI), copy `on_bytes` in-callback, hop to main, `onFirstFrame` once, `onEnd` forwards RAW (consumer sanitizes), `-dealloc` safety net (UAF guard if released without `-close`). Three pure Linux-tested Kit deciders in `Sources/SemicolynKit/ET/`: `ETConfig`+`validateETConfig`+`etEnvArrays`, `sanitizeEndReason` (Critical: strips ANSI CSI/OSC + whole `<...>` markup + control bytes), `mapETState`. macOS-CI bridge test drives ETSession vs a fake `et_client`. **KEY LINK LESSON:** first app consumer of ET symbols, so `libETerminal.a` pulled its transitive deps onto the app link line: needed `Clibsodium` (swift-sodium package) + `-lresolv` in the `Semicolyn` app target (`project.yml`); libsodium stays a separate slice, not merged into the xcframework. Memory `libetios-wrapper-landed-2026-08-04`.
   - **✅ `bootstrap` (slice 1b) MERGED to `main` (squash `0f1b422`, PR #117).** Four pure Kit deciders in `Sources/SemicolynKit/ET/`: `etGenerateCredential` (CSPRNG), `etBootstrapCommand` (`echo '<id>/<passkey>_<TERM>' | etterminal --verbose=N`), `parseETIDPASSKEY`+`ETBootstrapError` (Critical: reads the SERVER's `IDPASSKEY:<16>/<32>` from untrusted stdout, **corrects parent §3's "no response needed"**), `etConnectConfig`. `ConnectionViewModel.attachET`+`captureETBootstrap` run the bootstrap over the existing russh `openExec`. Secrets never logged. Memory `et-bootstrap-landed-2026-08-04`.
   - **✅ Transport picker + ET-interactive (§5 + input wiring) CI-GREEN, PR #118 OPEN (branch `feat/transport-picker`).** Explicit per-host `Transport` (SSH/Mosh/ET, default SSH) via `Inherited<Transport>` + pure `resolveTransport` (replaces "Mosh silently wins"; legacy-mosh migration + back-compat decode). ET now interactive: `etSession` arm in `sendTerminalInput` + `setETClientSize` + `onResize` sink. Chosen-transport failure -> `.failed(etFailureMessage(e))`, no silent fallback. Transport picker in host + Defaults editors. Full suite 1439/0; final review 0 must-fix. **This makes ET testable on device (see EXACT NEXT ACTION).**
   - **🔜 NEXT: the on-device ET test** (TestFlight build going up), THEN merge #118, THEN ET §4 dedicated error UI + roaming banner + the deferred picker-binding Kit-helper extraction. See EXACT NEXT ACTION at top.
   - Parent spec `docs/superpowers/specs/2026-07-10-et-transport-design.md` (branch `docs/et-transport-spec`, still UNMERGED). Open impl items: nonce-reuse code-read, xcframework size budget, roam-UX (reuse Mosh banner). Memories: `et-transport-brainstorm`, `et-ios-build-vendored`, `mosh-tmux-et-modes-roadmap`.
2. **Predictor prose-vocab** (user-requested 2026-08-02): seed regular/prose text into the DEFAULT prediction DB so prompting Claude yields suggestions; CLI vocab stays PREFERRED, prose is the fallback. Touch SeedKit + predictor ranking; keep the 8-layer secret-exclusion intact. Memory `predictor-add-regular-text-todo-2026-08-01`.
3. **Pad pane-management (Topic 6):** the arrow-cluster is arrows-only for now; the double-tap-zoom / long-press-split+menu slice is deliberately deferred (it would remove the only current long-press-zoom trigger before its replacement exists).

**Known contingency (device-passed, but watch):** if `keyboardLayoutGuide` ever reports a constant ~kbH residual in the `geo:layout` `gapToKeybar` field, the guide is excluding the inputAccessoryView; the one-line fix is `top - firstResponderKeybarHeight()`. Read the number, don't guess (memory `keybar-gap-post-switch-2026-08-02`).

**Session lessons:** SwiftTerm resolves to **v1.15.0** (the repo's `swiftterm-150/` clone is MIS-TAGGED 1.5.0 = wrong version; clone `v1.15.0` for truth); SwiftTerm rows = `frame.height/cellHeight`, no inset input; `setSelectionRange` end.col is EXCLUSIVE; the true cell size is `view.caretFrame` (not `bounds/rows`); TestFlight upload can hit transient network -1009 (rerun clears it).

Stray untracked (NOT part of any branch, leave or clean at will): `swiftterm-150/` (mis-tagged clone), `mockups/drafts/*`, `docs/superpowers/topics/2026-07-20-four-device-issues-resume.md`. (The `extern/eternaltermlib` + `extern/swift-sodium` + `extern/ios-cmake` dirs are now TRACKED submodules on main via #115, no longer stray.)

---

The canonical status + pending-work list. Architecture and the spec/plan map live in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md); the full decision log (incl. per-spec "out of scope") in [docs/brainstorming-decisions.md](docs/brainstorming-decisions.md).

**Headline:** design complete; a connect-and-get-a-shell MVP builds for the iOS Simulator. The protocol + logic tiers are built and Linux-tested; the app shell is built and validated by macOS CI. Not yet device-installable (needs Apple Developer signing).

**Tests green:** 17 Rust unit + 35 Rust integration (vs containerized `sshd`) + 1331 Swift (SemicolynKit + SeedKit), all on the Linux fast loop.

## Phase status

| Phase | Scope | State |
|---|---|---|
| **0, Foundations** | Cargo + SwiftPM workspace, UniFFI bridge proof, design tokens, data model, AES-256-GCM record envelope | ✅ Done |
| **1, SSH core** | russh behind UniFFI: handshake, host-key TOFU, auth (password / publickey / keyboard-interactive), OpenSSH cert auth, PTY shell, local/remote/dynamic forwards, ProxyJump, 4-tier algorithm allowlist | ✅ Done¹ |
| **2a, Storage core** | Host/identity schema + resolution table; `BlobStore`→`EncryptedRecordStore`, `SecretStore`, `HostKeyStore`; repository invariants; sync taxonomy | ✅ Done |
| **2b-i, Key minting** | ed25519 generation + OpenSSH-key import in the Rust core; `IdentityService` (mint→Keychain→metadata); `CoreIdentityMinter` bridge; publickey connect from a stored identity | ✅ Done (iCloud-Keychain flavor) |
| **2b-ii, Sync + SE** | CloudKit Private DB + sync engine; Secure-Enclave flavor (`SecAccessControl` + russh→SE signing bridge) | ⏳ enrollment-gated |
| **3, Terminal + tmux** | Control-mode stack done²; **Plans A+B+C+D done**: probe→attach `tmux -CC`, native pane layout, multi-window, debounced resize, raw-PTY degrade; terminal UX (bell, OSC 52, titles, URL tap, cursor, mouse dot, pinch-zoom); per-pane context detection (engine + `pane_current_command` poll + observable) + mid-session crash banner. | ✅ Done³ |
| **4, Keybar, input & predictor** | **4a + 4b + 4c done**: 4a mount + core input slots + keystroke codec + Ctrl-lock modifier SM + input router; 4b context promotions (bronze from `paneContexts`) + Fn mode (F1-F12, Fn SM, htop/top/mc auto-engage) + per-pane DECCKM; 4c predictor strip (input token tracker → engine suggestions → auto-hiding chip row, tap-to-complete, learn/harvest/flush, incognito-gated), all compile-validated on macОS CI (interaction/visual unverified pending a Simulator/device). **4d-1 done**: Codable keybar layout + sticky-rule mutations (reorder/remove/move-across-divider) + reverse-bar + persisted `KeybarSettingsStore` + Settings→Keybar list editor (Esc-pill long-press entry); pure core Linux-tested, editor macОS-CI-only. **4d-2 done** (PR #18): macro model + `{Ctrl+R}…{Enter}` template parser + custom-slot binding model (4 gestures, ≥1-binding rule) + `KeybarLibrary` folded into settings (back-compat decode) + macro→bytes expansion / `fireMacro` + `MacroRecorder`; App tier: searchable Launcher, custom-slot editor, template/record macro creation, wired "+ Add" sheet (core Linux-tested, App macОS-CI-only). Placeholders deferred to v2. **4e done** (PR #19): hardware-keyboard Cmd-shortcut catalog (15 actions / split + ⌘1-9 aliases) + resolver + ⌘-HUD titles; `compactKeybarSlots` + hide-with-hardware-keyboard setting; `TmuxCommand.killWindow`/`selectPaneRelative`; App tier: `perform` dispatch (window/pane/split/clear/paste/reconnect + launcher/settings/tips/new-connection sheets), GameController detection → compact/hidden keybar, `.keyboardShortcut` overlay, Tips screen, Settings toggle (core Linux-tested, App macОS-CI-only). Gaps: ⌘C native to SwiftTerm, ⌘F find deferred. | ✅ Done⁴ |
| **MVP app shell** | iOS app target + SwiftTerm wired via UniFFI: connect → password/keyboard-interactive/**publickey** auth → shell, real host-key TOFU trust | ✅ Builds for Simulator |
| **5-7, UI & ship** | Host CRUD UI + identity create/import + connect-from-saved done. Standalone Identities & Keys mgmt, connection-management UI, settings, IAP, App Store polish pending | ◐ Host CRUD done, rest pending |

¹ The `ssh-ed25519-cert-v01@openssh.com` **host**-certificate variant is deferred, blocked on russh 0.61 (verifies the server host key only as a plain `PublicKey`; no CA/principal/validity path). A guard test prevents advertising it until upstream support lands.
² Control-mode stack verified against real `tmux` in the `sshd` fixture, incl. the DCS-wrapped live `-CC` stream.
³ Plan D ships the context **engine + signal + observable** (`PaneContextMachine` dwell SM, `PaneContextStore`, `PromotionRegistry`/catalog, `list-panes` poll, `@Published paneContexts`); the keybar **visual** consumption (promoted slots, engage/disengage animation, per-pane pin, kill-switch) is Phase 4. Mirrors "predictor engine done / UI pending".
⁴ Phase 4 is feature-complete and CI-validated, but the whole keybar surface (4a-4e) is **interaction-unverified** pending a Simulator/device pass, see "Next". 4e device note: SwiftUI `.keyboardShortcut` resolves by character, so the shifted `⌘|` / `⌘?` bindings want a real-hardware check.

## Next (unblocked dev work)

### ⚡ Perf: tame the -CC render storm (FOLLOW-UP, tracked)

Same trace exposed a pre-existing perf/battery hit (NOT caused by the logging, which is zero-cost when
off): `layoutSubviews` fires ~60x/sec during a drag (31 `sizing:tmux` passes in 0.5s), each recomputing
the grid + measuring the font + `noteClientSize`. Driven by the ~1s `tmux context REPLY` poll + `%output`
churn. FIX (own session, MEASURED): coalesce/debounce `layoutSubviews`→`noteClientSize`, skip grid
recompute when bounds+cell unchanged, debounce the context poll. METRIC: the `sizing:tmux` frequency in a
trace is the render-storm gauge (before/after). Do this AFTER the swipe fix ships + is device-confirmed.

### 🔜 Tap-yield + window-switch-animation retest, branch `fix/altscreen-tap-yield`, PR TBD

Two items on this branch (ship together, next TF after the wheel merge). Wheel scroll is CONFIRMED working line-by-line in Claude under tmux -CC (build 55 trace: `drag-move keys=wheel`).

**1. Double/triple-tap yield (bug fix, `c8c8559`):** on a Claude/vim alt-screen (`.appOwnsInput`) or `.mouseReporting` pane, double/triple-tap should now do NOTHING (yield to the app) instead of drawing a garbage bottom-row selection. Verify: double/triple-tap on Claude = no selection; on a NORMAL shell prompt (`.localScroll`) word/line-select still works.

**2. Window-switch animation (spec/plan `2026-07-17-window-switch-transition`):** swipe horizontally between tmux windows (need >1). EXPECT: on release the current window slides OUT in the swipe direction immediately, then the new window slides IN from the opposite side when tmux delivers it (brief empty gap on a slow link is OK). Rightward swipe (previous): current exits right, new enters from left; leftward (next): mirror. Confirm the tab strip does NOT slide. Slow/failed switch: content must NOT stick off-screen (1.5s timeout snaps back). Log (Gesture): `window-switch anim: out=… in=… delta=…`.
   - Also RE-CONFIRM rendering unchanged by the `paneContentView` re-parent (Task 3): panes positioned correctly, halos/borders on the right panes, no first-mount flash, correct grid/cols. (Two ⚠️ items the review could only device-confirm.)

### 🔜 Wheel-scroll retest (the tmux -CC unknown), branch `fix/wheel-altscreen-scroll`, PR TBD

Alt-screen scroll reworked to a universal SGR mouse-wheel emitter (Blink's 1-line model); spec `docs/superpowers/specs/2026-07-17-wheel-altscreen-scroll-design.md`, plan `docs/superpowers/plans/2026-07-17-wheel-altscreen-scroll.md`. Rollback anchor: tag `tf54-known-good` = `99e90ff` (live TF build 54).

Enable Gesture logging (Settings → Diagnostics → Gesture). Drag to scroll a **Claude** pane:
- **EXPECT:** `drag-move keys=wheel runs=… coord=(c,r)` in the trace, and Claude's transcript scrolls ~**one line per line-height** of drag (crisp, not half-screen jumps).
- **IF Claude does NOT scroll:** our synthetic wheel bytes are not reaching the app under `tmux -CC` (the known risk). Switch Settings → Experimental → Alt-screen scroll to **"Fallback"** and confirm PgUp/arrows still scroll (isolates the -CC-drops-wheel case). If wheel fails, **plan B = the local-scrollback-buffer project** (memory: `tmux-cc-scrollback-architecture`).
- Also verify **less/vim** scroll one line per line-height in wheel mode.
- Settings migration: existing testers had `altScrollMode` = a legacy value; confirm it silently became "Line scroll" (no settings wipe, no crash).

### 🔜 Logging rework retest (decision-point logging, branch `fix/altscroll-b-context-and-logging`)

Self-contained decision-point logging shipped (spec `docs/superpowers/specs/2026-07-16-decision-point-logging-design.md`, plan `docs/superpowers/plans/2026-07-16-decision-point-logging.md`). Each gesture/alt-scroll decision now emits ONE self-contained line (inputs + output + reason); session/user-action markers added; `gesture` category is now default-OFF.

**Before the scripted drag retest**, enable in Settings → Diagnostics: **Gesture** (Tmux + Lifecycle are already on). Then run the scripted actions. The trace should read top-to-bottom without narration:

```
=== session-start build=N session=… ===
user-action: mode-switch <mode>
alt-scroll pane=%N mode=… app=… → keys=… reason=…
drag-begin winner=… imode=… mode=… app=… → keys=… reason=…
drag-move keys=… runs=… sent=… total=…
drag-end owner=… imode=… emitted=… outcome=<scroll|pageKeys|arrows|none>
```

Confirms B mode-classification (mouseReporting vs appOwnsInput) from `drag-begin imode=` + `alt-scroll … reason=`. THEN fix B-remainder (mouseReporting + AI-CLI → PgUp/PgDn), C-momentum (flick/inertia), D-width (quantize zoom). Deferred sweep (§4c): adopt `decisionLine(...)` at tmux-grid / context-poll / transport boundaries.

### ⏸ In progress, resume here (paused 2026-07-07 for context-clear)

- **Device-testing round (build 21, `0.1.0`), 4 fixes SHIPPED + CONFIRMED on device; 1 fix awaiting re-test; 2 topics OPEN.** A device pass on build 20 surfaced bugs; all root-caused via an on-screen debug panel (now gated in Settings → Diagnostics, off by default).
  - **CONFIRMED working on device (build 20):** ① tmux **reattach no-echo**, `windowListingEvents` never set the window's `activePane` so `send-keys` had no target → keystrokes dropped; fix emits `windowPaneChanged` from the layout's first pane (`main` `5b9fccb`, Kit test `testApplyEventsSetsActivePaneFromLayout`). ② **predictor learned ANSI/color codes**, `harvest` split output on whitespace only; fix `stripANSI` (CSI/OSC/C0) before tokenizing. ③ **keybar armed Ctrl/Alt/Shift didn't apply to keyboard-typed chars** (only keybar keys), route SwiftTerm keyboard input through `router.keyboardInput`; cursor-drag arrows keep a raw path. (The earlier #49 tmux-attach-layout-prime also merged, `046aa22`.)
  - **AWAITING RE-TEST (build 21, `221fb74`):** ④ **input felt sluggish**, self-inflicted: `DebugLog.log` ran on every keystroke (string interp + `Date()` + `@Published` append + os_log), some BEFORE the write, violating Plan B's write-first. Fixed, `DebugLog.log` is now an `@autoclosure` + no-op when diagnostics off; buffer no longer `@Published`; `sendTerminalInput` restructured write-first with a SACRED-PATH guard comment. An input hot-path audit confirmed all 8 input surfaces already funnel through the single `sendTerminalInput` choke point (no parallel paths), this hardens it. **Re-test typing snappiness on build 21.**
  - **OPEN #1, first-connect failure (NOT diagnosed).** The very first connect attempt fails; only a subsequent reattach gets a (previously broken, now fixed) terminal. Not yet root-caused. **To debug:** turn on Settings → Diagnostics → Show debug log panel, reproduce a fresh connect, 🐞 → Copy → the connect path is instrumented (`connect(saved): START/…/auth/…/attachSSHShell decision`, every `.failed` branch). Paste that trace to root-cause.
  - **OPEN #2, cursor-centric interaction redesign (SPEC'd + approved, not implemented).** `docs/superpowers/specs/2026-07-06-cursor-centric-interaction-design.md` on branch `feat/cursor-centric-interaction`. Removes the always-on halo (device feedback: awkward, too bright, disappears after a line), makes it iOS-native: **tap = reposition** (same-line reliable via arrows), **quick-drag = scrub cursor**, **hold-then-drag = native selection + loupe**. Supersedes the locked halo decision. Also noted on device: the single-pane **active-pane focus border reads as a pointless coral rim**, fold a "mute/hide the border when only one pane" tweak into this work. Next step: `writing-plans` → implement.
  - Debug tooling (`DebugLog`/`DebugLogPanel`) is retained + gated (Settings → Diagnostics); keep until OPEN #1 is closed, then remove.

- **Faster verification (front-load before the ~40min CI/TestFlight loop), SPEC'd + Plans A & B SHIPPED to `main` 2026-07-06.** Spec `docs/superpowers/specs/2026-07-06-faster-verification-testable-seams-design.md`. Audit found the App tier already ~86% legitimately Apple-coupled (~5% pure/extractable; most decision logic already in Kit), so the move was surgical, not a big sweep. **Plan A** (`e829bf1`): extracted the last trapped pure unit (`WindowNavigation.stepIndex` wrap math + BVA tests) + a CI `lint` **View-only gate** (`scripts/check-app-view-only.sh`) that keeps pure logic out of `App/`. **Plan B** (`fae0807`): forked the predictor off the sacred keystroke send-path, `PredictorInputFilter` + `SuggestionRefreshCoalescer` (Kit, tested) + `PredictorActor` (off-main FIFO consumer) + coalesced suggestion refresh + first `os_signpost` keystroke-latency measurement (`docs/perf-measurement.md`) + `predictorSuggestions` → `PredictorViewModel` slice. **Deferred:** Docker-OSX ruled out (Apple EULA; no nested-virt) and xtool can't run the iOS Simulator on Linux (its own docs), behavioral App-tier verification still needs a Mac; the ViewModel split stopped at the predictor slice (6a), banner/tmux slices (6b/6c) deferred as non-jank-sources wanting local Mac iteration. **On-device pass still owed:** confirm typing snappiness + suggestions populate + passwords don't learn + capture the `os_signpost` trace. Memory: `faster-verification-brainstorm`, `xtool-evaluation`.

- **Mosh transport (real interop)**, **M1 + M2 + M3 + M4 SHIPPED + merged to `main`** (2026-07-02/03); Mosh is wired into the live connect flow with pre-first-frame SSH fallback. **Only the device/Simulator + TestFlight manual pass remains.** Specs/plans: `docs/superpowers/specs/{2026-07-02-mosh-transport-design,2026-07-03-mosh-m3-m4-bridge-wiring-design}.md` + plans `{2026-07-02-mosh-transport,2026-07-03-mosh-m3-m4-bridge-wiring}.md`.
  - **M2 (merged `7e8cb95`, PR #31-era):** pure Linux-tested Kit units, `parseMoshConnect` (MOSH CONNECT parser), `moshServerCommand` (bootstrap argv), `moshLaunchDecision` (mosh-vs-SSH), plus a Rust `open_exec → mosh-server new → MOSH CONNECT` interop test against a real mosh-server (Alpine sshd fixture gained `mosh`).
  - **M1 (merged `18ba374`, PR #36):** `blinksh/mosh` vendored as a pinned submodule (`extern/mosh` @ `3640d366`) → `scripts/build-mosh-xcframework.sh` cross-compiles **protobuf 3.21.12 + ncurses 6.5 + Mosh** (crypto = Apple CommonCrypto, so **no OpenSSL/Nettle**) into `Mosh.xcframework` (device arm64 + arm64/x86_64 sim); linked as a `#if os(macOS)` binaryTarget in `Package.swift` and built by macOS CI. **CI green:** framework builds/links + exports `mosh_main`; app `BUILD SUCCEEDED`. Took 14 CI iterations resurrecting the archived Blink iOS build for Xcode 16.2 / iOS-18 SDK.
  - **M3 (SHIPPED, PR #37):** `MoshSession` Obj-C++ bridge + `ConnectionViewModel` Mosh branch (bootstrap → `moshBranchOutcome` → `MoshSession` → SwiftTerm I/O), pre-handoff SSH fallback + `MoshFallbackBanner`. Bridge drives the real `mosh_main` over a pipe pair (unbuffered `fout`, mosh + reader threads, `pthread_kill(SIGWINCH)` resize) with a single-mutex/join-before-close teardown; a macOS-CI `SemicolynBridgeTests` target drives it against a fake-`mosh_main` loopback. Also fixed M1's `Mosh.xcframework` to ship a **self-contained** archive (object-explode merge, `libtool` basename-dedup fix) + link `libz`. ~11 CI iterations.
  - **M4 (SHIPPED, PR #38 → main `4cdb9a1`):** pre-first-frame SSH fallback, `MoshSession` gains `onFirstFrame` (fires exactly once on the first output byte = UDP handshake completed, gated under `_lock`, cleared by `-stop`); `ConnectionViewModel` tracks `moshFirstFrameSeen` so `onEnd` **before** a frame (UDP blocked / crypto mismatch) falls back to SSH on the **same retained connection** with a *"Mosh UDP unreachable"* banner (instead of dead-ending to `.failed`), while `onEnd` **after** a frame reuses the mid-session crash banner. Shared tmux/raw connect tail extracted into `attachSSHShell(conn:host:defaults:)`. Also verified: no `scenePhase` transition calls `stop()`/`teardown()`, so Mosh's intrinsic roaming/background-foreground is never interfered with (Mosh reconverges on its own; a true socket kill → `onEnd` → crash banner is the accepted v1). macOS-CI bridge test asserts `onFirstFrame` fires exactly once across all output bytes.
  - **TestFlight (M1-M4) UPLOADED 2026-07-03** (run 28676499604, `UPLOAD SUCCEEDED`, Delivery UUID issued): first Mosh-capable build on TestFlight. Getting there fixed the release lane (build `Mosh.xcframework` + recursive submodules), the export-compliance 409 (self-classified **mass-market ECCN 5D992.c**, `ITSAppUsesNonExemptEncryption=NO`, see `docs/export-compliance.md`; keeping russh = paperwork-free), and a CI bug where altool reported failed uploads as green. Confirm "Ready to Test" in the TestFlight app (async processing).
  - **Post-TestFlight device-pass fixes (merged 2026-07-03/04):** first device testing surfaced three bugs, all fixed on `main`. **PR #39 (`bdc45f7`), key-aware credential resolution:** `SessionView.resolveCredentials` only checked for a stored password, so a key-configured host was forced onto a password screen captioned "key auth coming in 2b" (the backend has done publickey since 2b-i). New Kit helper `credentialResolution(hasUsableKey:hasStoredPassword:)` mirrors `authenticate`'s precedence (usable key > stored password > prompt); a key host now connects via key with no prompt. **PR #40 (`d4f4948`), terminal blank-screen + no-keyboard on the raw/mosh path:** (a) Mosh's one-shot first framebuffer frame was dropped when it arrived before `TerminalScreen` installed `output.onBytes` (no buffering → permanent blank) → new `PendingOutputBuffer` (pure, Kit-tested) buffers pre-sink bytes and flushes on install, mirroring tmux's `pendingPaneBytes`; (b) `TerminalScreen` never called `becomeFirstResponder()` (only the tmux path did) so no keyboard on raw SSH **and** mosh → now claims focus once in `updateUIView` when in-window. Two codebase-wide pattern sweeps confirmed each bug exists in exactly one place.
  - **NEXT, M4 manual/device pass (yours):** re-test on the build that includes #39/#40, mosh happy-path (terminal renders + keyboard raises), key-host connects via key with **no** password prompt, mosh-server-absent SSH fallback, mosh-disabled straight-to-SSH, **UDP-blocked → "Mosh UDP unreachable" fallback (M4)**, roaming Wi-Fi↔cellular + background/foreground feel. Then stretch macOS real-server UDP interop test (Task 8). Tasks 6-8 of `2026-07-03-mosh-m3-m4-bridge-wiring.md`.
- **On-device testing via TestFlight**, CI lane prepped + dormant (`.github/workflows/release-testflight.yml`, gated on repo var `TESTFLIGHT_ENABLED`). No personal Mac needed: the GitHub macOS runner archives/signs/uploads via an App Store Connect API key. Enable once the paid Dev Program enrollment completes, one-time steps + secrets in `docs/testflight-setup.md`. Unblocks every owed device feel-pass below.
- **App icon, SHIPPED** (2026-06-30). Mark = a shell-prompt chevron `❯` fused with a comma ("prompt + semicolon"): coral prompt `#FF6F5E` + off-white "text" comma `#CFD6E4` on midnight `#12151F`, flat. Vector master `assets/semicolon-mark.svg` (two paths, theme/glow-able); 1024² opaque PNG wired into `App/Assets.xcassets/AppIcon.appiconset/icon.png` + `Contents.json` filename. Re-theme/re-size any time via `scripts/logo_render.py` (recolor + uniform-glow + multi-size pipeline). The interactive color playground + final renders live in `mockups/drafts/` (+ `logo-renders/`). Follow-ups: per-shape **neon-glow** variants are for marketing/site, not the icon; **alternate/Pro app icons** still tracked under the StoreKit slice. **Icon spacing tweak, DONE (2026-07-07):** widened the vertical gap between the `❯` chevron and the `,` comma (they used to touch) via a symmetric ±24-unit nudge (chevron `translate` Y `-1023.6→-1047.6`, comma `-568.2→-544.2`), midpoint unchanged, so the mark stays vertically centered. Re-rendered the 1024² flat opaque-midnight PNG (`scripts/logo_render.py assets/semicolon-mark.svg -b '#12151F' --no-glow -s 1024`) into `App/Assets.xcassets/AppIcon.appiconset/icon.png`.
- **Cursor-placement Simulator feel-pass** (needs a Mac; App-tier compile-validated only), the gesture is built but UNTUNED. Verify/tune on a Simulator/device: halo rides the cursor on the focused pane (and its refresh cadence during fast output); drag moves the cursor in readline/vim incl. under `mouse=a`; **gain curve** (1:1 ≤600pt/s → 3× cap) + **1.5-cell vertical dead-zone** feel; **gesture coexistence**, the halo-gated pan must NOT block scroll / window-switch / long-press-select outside the ~60pt halo; engage/lift haptics. Then build the deferred slices once their SwiftTerm APIs are confirmed live: **loupe magnifier** (read row chars via `getChar`, render with `LoupeWindow`), **offscreen `⌖` indicator** (`isOffscreen` + scroll-to-live), **selection-handle suppression** (set `CursorDragController.suppressed` from SwiftTerm's selection-active state). Tunables live in `CursorDragEngine` statics + `CursorDragController.haloRadius`.

- **Phase 4, keybar, input & predictor: DONE** (4a #14, 4b #15, 4c #16, 4d-1 #17, 4d-2 #18, 4e #19). Follow-ups carried forward (deferred, noted in code): **4d-2**, parameterized macro placeholders (`{{host}}`, defaults, per-host remembered values, the v2 placeholder system); editing an existing macro's body from the Launcher; live-keybar capture for record mode (v1 uses a dedicated input pad). **4e**, ⌘C copy left to SwiftTerm's native path; ⌘F find-in-scrollback **dropped** (PR #25): tmux copy-mode search covers the persistent-session workflow and SwiftTerm ships no find API, so the dead shortcut was removed from the catalog (re-entry pointers in the external-keyboard spec's deferred section). Specs: `2026-06-15-keybar-customization-design.md`, `2026-06-17-external-keyboard-design.md`.
- **Simulator/device pass on the keybar (4a-4e)**, compile/CI-validated only; interaction + visuals unverified. Verify: mount-above-keyboard (`inputAccessoryView` vs the v1 `safeAreaInset`), slot recolor, gestures, predictor-strip slide/chips/tap-complete; **plus the unrendered spec visuals**, promoted-slot bronze top-edge accent (context spec §"Promoted slot visual") and Fn-locked brighter glyph + 4pt lock-dot (function-keys spec §"Visual"). **4d-1 editor**: Esc-pill long-press → Settings→Keybar; verify drag-reorder, swipe/edit-mode delete (Esc/Pad locked), per-row move-across-divider menu, reverse-bar mirror (`layoutDirection` flip), Reset, note 4d-1 used two sections + a per-row move action instead of the spec's single draggable divider (SwiftUI cross-section drag is unreliable), revisit. **4d-2 surfaces**: Launcher (search/pin/delete), custom-slot editor (4 binding rows, ≥1-binding Save gate), macro creation (template live-parse + record chip list), pinned-macro / custom-slot rendering (hint glyphs, `fireMacro`), "+ Add" sheet. **4e surfaces**: hardware-keyboard → compact/hidden keybar (GameController), the ⌘-HUD + each Cmd-shortcut firing (esp. shifted `⌘|`/`⌘?`), held-modifier passthrough, Tips screen, the hide-keybar Settings toggle. Gated on Apple enrollment for device.
- **Pro / StoreKit slice** (deferred from the theme picker), the real one-time purchase: StoreKit purchase + restore + Family Sharing, the Supporter badge in About & Help, and alternate app icons. The theme picker shipped against a stub `ProStore` entitlement seam; this slice swaps the backing in behind that same surface. Spec: `2026-06-16-pro-paid-scope-design.md`. Partly enrollment-gated (App Store Connect product config).
- **Theme-picker Simulator pass** (App-tier, CI-validated only), verify on the Simulator: Settings gear → Appearance → Theme renders both rows with swatches; Bell Bronze shows ✦Pro and routes to the upgrade placeholder without changing selection when Pro is off; debug-unlock then selecting Bell Bronze recolors chrome live; selection persists across relaunch. **SwiftTerm live-recolor:** WIRED 2026-07-01 (PR #31), `applyPalette` re-installs the terminal's 16 ANSI colors + fg/bg/cursor/selection on theme change via `updateUIView`; the Simulator pass now just visually confirms the terminal *content* repaints on switch (chrome already recolors via `@Environment`). Minor cleanup carried: `HostListView` uses two `.navigationBarLeading` `ToolbarItem`s, fold into a `ToolbarItemGroup`.
- **Phase 3c deferred seams**, `onSSHLink` connect-prefill and active-pane title keying **SHIPPED** 2026-06-28 (PR #26, `8159455`): ssh:// link tap → `QuickConnectSheet` confirm → nested session (pure `parseSSHURL`); tmux window title now keyed to the active pane (pure `titleToPublish`/`titleOnActiveChange`). **Halo-suspend resolved** by the cursor-placement feature (PRs #27/#28): the halo now exists, mouse-mode deliberately does NOT suspend it (we synthesize arrows, not mouse events), and both `TODO(phase4)` markers are gone; the residual selection-handle suppression is tracked under the cursor-placement Simulator pass above. _3c Simulator pass (needs a Mac) still owes: confirm the ssh:// confirm-sheet → fullScreenCover transition doesn't flicker, and the title follows the active pane on `⌘]`._
- **Pre-release code + security review, RUN 2026-07-04; all 9 remediation items DONE** (branch `fix/ssh-keepalive`, PR #41). Whole-codebase audits via the two reusable skills at `~/.claude/skills/{full-code-review,full-security-review}` (Fable orchestrator + fresh-context Opus probes + independent adversarial verifier). Full evidence + proving paths in the (gitignored, regenerate-on-demand) reports: **[`.code-review/REPORT.md`](.code-review/REPORT.md)** (14 correctness findings, 38 candidates dropped) and **[`.security-review/REPORT.md`](.security-review/REPORT.md)** (1 security finding, 3 dropped). Both audited at commit `2f21bc0`. The security review's headline result is how much *held*, host-key trust fails-closed on dismiss, no silent auth downgrade, algorithm downgrade gate holds under all toggle states, at-rest AES-GCM + secret data-flow clean, `MoshSession.mm` memory-safe, parsers hardened against hostile server bytes. All 9 fixes shipped (3 Critical + 6 Important); Linux fast loop green (911 Kit/SeedKit + 23 Rust lib + 9 auth-integration), macOS CI validated the App-tier + FFI-signature changes.

  | # | Sev | Area | Fix shipped | Status |
  |---|---|---|---|---|
  | 1 | 🔴 Critical | `connection.rs` `prepare()` | `KeepaliveConfig` FFI record wires `serverAlive*` → russh keepalive; `inactivity_timeout` becomes a derived backstop `interval×(count_max+3)` (=180s), strictly above the `×(count_max+1)`=120s keepalive death point so keepalive decides. `interval 0` disables keepalives. New `ConnectError::Timeout` + a 20s handshake-timeout wrapper for unreachable hosts. `textToInheritedInt(minimum:)` lets `0` be typeable for serverAlive fields (folds in K9). | ✅ `03dc4a5` |
  | 2 | 🔴 Critical | `ConnectionViewModel` mosh `onOutput` | `sess.onOutput` routed through `output.onOutput(data:)` (the buffered path) so the first framebuffer diff isn't dropped → no more blank mosh terminal. | ✅ `7f57df2` |
  | 3 | 🔴 Critical | `MoshSession.mm` `stop()` | Join the reader **before** closing `_outPipe[0]` (normal path); degenerate no-mosh-thread path closes the write end for EOF first. Corrected the header comments; noted the self-pipe upgrade path. A subagent audit confirmed this was the codebase's only close-before-join instance. | ✅ `7f57df2` |
  | 4 | 🟠 Important (sec) | Predictor input path | New `PasswordEntryDetector` (pure Kit, 18 adversarial tests): OR-combines echo-inference (a line whose typed chars aren't echoed back → password) + prompt-text (`password:`/`passphrase`/`[sudo] password for`/`verification code:`), biased toward suppression. Fed both output taps + input; gates learning at the record boundary. **Approach diverged from the report sketch**, SwiftTerm exposes no echo/SRM flag and SSH doesn't signal remote termios (research-confirmed), so heuristic detection is the only option. Predictor is local-only today (no CloudKit sync yet), so this closes a local leak before sync ships. | ✅ `6e435b8` |
  | 5 | 🟠 Important | `ConnectionViewModel` mosh resize | `TerminalScreen` gained an `onResize` sink (mirrors the tmux `onTmuxResize` convention); `SessionView` routes it to `setMoshClientSize` when `isMoshActive`. 80×24 seed corrected by the first resize event (future note left for plumbing the real initial grid). | ✅ `7f57df2` |
  | 6 | 🟠 Important | `ConnectionViewModel` onEnd/`sendTerminalInput` | Post-frame `onEnd` now `stop()`+nils `moshSession` + resets `moshFirstFrameSeen` before the crash banner, symmetric with the pre-frame branch, keystrokes after a banner-reattach reach the live tmux/raw session instead of the dead mosh pipe. | ✅ `7f57df2` |
  | 7 | 🟠 Important | `Resolution.swift` nested resolve | New `resolveLeaf` helper resolves each nested-config leaf host→Defaults→builtin independently; a host setting one leaf no longer shadows Defaults values for the leaves it left unset. 4 regression tests. | ✅ `0b9d5a0` |
  | 8 | 🟠 Important | `connection.rs` kbd-interactive | Returns typed `AuthOutcome::Failure` when responses are exhausted on a non-empty prompt round (fail-fast), instead of a short batch that trips the reply-count mismatch → transport drop. Zero-prompt rounds still answered with an empty batch. | ✅ `0b873d0` |
  | 9 | 🟠 Important | Rust auth tests | Closed all 5 gaps: publickey wrong-key negative (minted unauthorized key → Failure), malformed-key typed error, empty-password + empty-kbd-interactive boundary (also regression-guards #8), encrypted-key **valid** path (round-trips to the same material), and removed the mock-only tautology test. 9 auth-integration green vs the real sshd fixture. | ✅ `0b873d0` |

  Not actioned (recorded): **OSC 52 remote clipboard-write defaults ON** (paste-hijack surface) is a *risk-accepted* documented design choice (per-host toggle + "turn off for untrusted hosts" ships), in the security report, no action. Minor correctness cleanups (dead `resolveTailscaleRequired`/`PromotionCatalog.load`, stale `TODO(phase4)` comments, duplicated `handlePinch`) are in the code report's dropped-but-noted section, batch when touching those files. **Follow-up idea for #5:** plumb the real initial terminal grid into `attachMoshIfPossible` to skip the brief 80×24 first frame (Q2(b), noted in-code).

## Enrollment-gated (Apple Developer Program org enrollment, in flight)

- **2b-ii**, CloudKit Private DB sync engine + Secure-Enclave identity flavor.
- **On-device / TestFlight testing** and code-signing, needs the signing identity.

## Deferred / when-needed

- **Accessibility review**, app-wide a11y pass (VoiceOver for terminal + keybar, Dynamic Type vs fixed cell font, low-opacity overlay + focus-border contrast, Reduce Motion for bell pulse / cursor blink, haptic opt-out, tap-target minimums). Best once terminal UX + keybar/Settings UI exist.
- **Nerd Fonts**, patched glyph fonts (powerline/dev icons) in the terminal renderer, if users hit missing glyphs. Ties into a Phase-4 Terminal Settings font picker.
- **Selection drag-handles (adjust an existing selection), FUTURE (requested 2026-07-20).** After a double/triple-tap selects a word/line, let the user drag either END of the selection to extend or shrink it. Blink's affordance: a round dot handle at the top of the left (start) edge and the bottom of the right (end) edge; dragging a handle moves that boundary while the other stays anchored. Brainstorm scope: how to detect an active selection + its endpoints (SwiftTerm selection API surface, cf. the deferred "selection-handle suppression" note under Cursor-placement drag, which flagged there's no public selection-active signal; this needs the inverse, a way to READ selection bounds), the two draggable handle views + their hit-targets, the boundary-update math (cell under finger → new selection anchor/head, clamped, with the opposite end fixed) which should be a pure Kit engine (Linux-tested, like `CursorDragEngine`), and interaction with the existing cursor-drag halo + alt-screen mouse forwarding (a selection-handle drag must not be read as a scroll/cursor pan). Ties into the cursor-placement drag work and `editingInteractionConfiguration=.none` (which disabled UIKit's own selection UI).
- **Pull-down / per-key secondary character (keybar), FUTURE BRAINSTORM (requested 2026-07-06).** Explore giving each keybar key a "pull-down" secondary input (iOS-keyboard style: a short downward drag / long-press on a key emits an alternate character or sequence, e.g. a number-row key pulling down to a symbol, or a letter to its punctuation). Brainstorm scope: which keys get a secondary, how the secondary is chosen/configured (fixed vs user-editable, cf. the existing custom-slot editor), the gesture (pull-down vs long-press vs both) and its interaction with the existing keybar gestures (Fn tap/double-tap, promotion slots, cursor-drag), and the pure input-mapping logic (key + gesture → bytes) that should live in Kit and be Linux-tested. Ties into the Phase-4 keybar (`docs/superpowers/specs` keybar design) and `KeybarLayout`/`MacroTemplate`.
- **russh host-cert gap**, see footnote ¹; blocked upstream.
- **tmux -CC hardening backlog (iTerm2-derived, researched 2026-07-14).** iTerm2 is the tmux control-mode reference impl (macOS-only, no iOS port to reuse: we port the *design*). Full set of portable correctness patterns our -CC gateway likely lacks (agent memory: `iterm2-tmux-cc-design-lessons`). Each is a separate future audit/hardening item, NOT part of the current alt-screen fix:
  1. **Reply-correlation abort-cascade.** We do FIFO reply correlation (`TmuxSessionController.pending`), but a `;`-joined command LIST aborts entirely server-side after the first error and tmux sends NO `%begin` for the skipped commands: the client must synthetically fail the remaining queued commands or the FIFO desyncs permanently. AUDIT: do we ever send `;`-joined lists? If so, add the cascade.
  2. **`%pause`/`%continue` (tmux >= 3.2) = full per-pane RE-CAPTURE on unpause**, not "resume streaming" (tmux drops buffered output during pause). Opt-in via `refresh-client -fpause-after=<sec>`; on `%continue` re-run the whole per-pane attach fan-out. Ties into `tmux-cc-scrollback-architecture` memory.
  3. **No-resume reattach architecture.** Clean detach and server death both surface as `%exit` (indistinguishable): wipe queue, close panes, fail pending callbacks; make full re-attach cheap + idempotent; persist UI state server-side as tmux user options (`@semicolyn_*`). tmux 3.2+ needs a `\n` nudge after `%exit` to close the pipe.
  4. **Scrollback seeding = two decoders + the tmux backslash-escape bug.** History = `capture-pane -peqJ -S -<n>` (`-N` gated >= 3.1, `-a` for the alternate buffer); `-e` output is REAL terminal output to REPLAY THROUGH THE EMULATOR (feed each line through a VT100 instance), not text-with-escapes-to-strip. Live catch-up (`capture-pane -p -P -C`) uses a separate octal parser. THE BUG: tmux escapes control chars but NOT backslashes, so only treat `\NNN` as an escape when the octal value is < 0x20 (a real C0), else literal user backslashes get corrupted; also skip stray `\r` mid-escape.
  5. **send-keys 3-tier encoding + modifyOtherKeys C0 trap.** `send -l` (ASCII allow-list) / hex `send 0xNN` / literal-byte `send -H NN` (>= 3.0a, for C0). On tmux >= 3.5, plain hex for 0x00..0x1F becomes literal TEXT once a pane enables `modifyOtherKeys` (use `-H` for C0). We already use `send-keys -H <hex>` (good): verify C0 behavior under modifyOtherKeys. Cap commands at 1000 bytes (> 1024 crashes tmux 1.8).
  6. **Handshake gate for the `%output`-before-pane-exists race.** Dispatch all `%`-notifications only after the initial attach batch completes (iTerm2 `acceptNotifications_`); post-handshake unknown-pane output is a logged no-op, not an error.
  7. **Layout parsing gotchas** (`%layout-change` / `#{window_layout}`): ignore the 4-hex leading checksum (parse from offset 5); `[` = horizontal, `{` = vertical; manual balanced-bracket scan (regex cannot match nesting); split children on TOP-LEVEL `,` only (leaves contain their own commas); coalesce same-orientation; normalize root to a splitter.
  8. **Version-encoding for feature gates:** `3.0a -> 3.01`, `3.0b -> 3.02` (suffix -> hundredths); `send-keys -H` is 3.0a not 3.0, so gating must be exact.
  9. **Hard-won bug notes:** tmux 1.8 `%exit` can arrive with no closing `%end` (synthesize it); unconditional `copy-mode -q` at attach (a broken tmux.conf can leave a fresh session in copy-mode silently eating keystrokes, iTerm2 issue 3193); always unzoom on attach (tmux 2.1 cannot report the zoomed pane); `\t` vs literal `\\t` regressions in `list-windows` TSV on dev tmux snapshots (defensive fallback parsing, not just version-gating).

## Resolved (recent)

- **Window-switch keybar-gap arc, MERGED 2026-07-25→28 (PRs #104-#111 → `main`; head `991bf5b`).** A multi-round device-debugging arc against a "blank/gapped pane after switching tmux windows" bug; device-confirmed fixed. The final root cause was NOT what the first seven fixes chased (seed/scroll/geometry symptoms): an isolation test (raw SSH + manual `tmux attach`, no `-CC`) had **no gap**, proving the bug was 100% in the `-CC` render path (`TmuxPaneContainer`), not tmux/SSH/SwiftTerm. A `geo:pane` diff (logging added to the shared `PaneTerminalView` so both the raw and `-CC` paths log) showed the difference: the raw path sizes its terminal to the FULL height and lets the keybar (an `inputAccessoryView` floating in its own window) overlap the bottom rows; `-CC` subtracted the keybar height (`usableH = bounds − kbH`), reserving a keybar-height band INSIDE the ContainerView that the floating keybar never fills → dead-black gap ≈ 56px. **Fix (#110):** `-CC` grid + pane frame now come from the FULL `bounds.height`, matching the raw path (SwiftTerm derives its row count from `frame.height / cellHeight` and ignores `contentInset`, so full-frame + full-grid is the only self-consistent no-gap layout). **Follow-up (#111):** removed the `bounds.size != lastLaidOutBounds` gate in `layoutSubviews` so the FIRST window re-fits to the settled container height immediately (it previously lagged ~kbH behind the keybar until a switch's `apply()` re-framed it). Earlier PRs in the arc (#104 reseed-on-switch, #105 drop trailing-newline in `reconstructHistory`, #107 single capture-pane per switch via `forgetSeedState`, #108 keep-keyboard-up-on-switch) fixed real adjacent bugs (blank pane, stray prompt line, double-seed, keyboard flicker); #106/#109 bottom-align attempts were superseded by the real fix. **Kept:** a new default-on `.geometry` LogCategory (`geo:makeUIView` / `geo:layout` / `geo:pane` / `geo:raw`) that captures container bounds, cell, grid, per-pane frame + scroll-view content geometry + view identity, it found this bug and is a durable layout-diagnostics layer (also useful for the font-stuck-at-8pt item). Kit 1331 green; App macOS-CI-validated. **Log-capture gotcha:** device diagnostics stream to the `tools/syslog-sink` docker service (must be up); prefer TLS 6514 / TCP over UDP (UDP re-parses and concatenates the long geo lines). **Still separate/open:** font stuck at ~8pt from a stale persisted pinch-zoom; selection-when-scrolled `cell(at:)` yDisp bug.
- **Swipe live-drag + device rendering round, MERGED 2026-07-24 (PR #103 → `main` `b556578`, squashed).** The `feat/finger-drag-window-transition` branch accumulated the whole live-drag + device-testing round; all issues device-confirmed before merge. Fixes: **live-drag** (position via `bounds`+`center` when the transform is non-identity, so `layoutSubviews` no longer stomps the drag translation); **KISS window switch** (deleted the animated-switch machinery, swipe→select-window→redraw on release, `docs/superpowers/specs/2026-07-22-kiss-window-switch-design.md`); **terminal staircase** (root cause was NOT cell-width, it was seeded history joined with a bare LF; SwiftTerm's `lineFeedMode` defaults off so `\n` didn't return to col 0 → `reconstructHistory` now joins with `\r\n`, plus `capture-pane -J` to un-wrap wide history); **predictor screen-jump** (the predictor strip was a conditional row that grew the input accessory → now an always-reserved constant 18pt row, one-line, `PredictorStripView`); **tighter input area** (keybar keys 34→27pt, bar padding 3→2pt); **keybar over-measure** (the combined `host.sizeThatFits` fed its own frame back, toggling 56↔90 → measure strip + keybar via off-screen never-framed hosts and sum); **gap below the terminal** (pane frame height came from tmux's lagging `%layout` → new pure `fitPaneRects` stretches the layout to fill the real usable area). Diagnostics added during the round (cell-probe, `.sizing` category) were removed before merge. Kit-tested (1327 green); App tier macOS-CI-validated. **Still separate/open:** font stuck at ~8pt from a stale persisted pinch-zoom (may want a font-size reset affordance); selection-when-scrolled `cell(at:)` yDisp bug (agent memory `gesture-arch-rethink-2026-07-21`).
- **Blue themes (Piece 2)**, shipped (Kit tier) 2026-07-02: **Neon Cobalt** (cool-electric, accent `#5A6EFF`) + **Glacier** (cool-soft pastel on a lighter slate, accent `#8AA6E8`), both Pro-gated (`Theme.catalog` entries 3 & 4). 849 Kit tests green; App surfaces them via the catalog (macOS-CI-validated). **Reframe from the original plan:** themes are distinguished by a full-palette *personality* (temperature × intensity), not just the accent hue, the "even-spaced azure / Azure Prism" idea was dropped. **Theme format decided:** a theme = a standard **iTerm2 `.itermcolors`** palette (16 ANSI + fg/bg) + one `accentSlot` + optional chrome overrides; surfaces/text/highlight are **derived** (surfaces = bg tint-steps, highlight = lighten(accent), status = red/green/yellow slots), Bell Bronze the one theme keeping hand-authored chrome. Pipeline `mockups/drafts/themes/build.py` parses iTerm2/base16 → derives → emits the (generated) gallery config + `Theme.fromANSI` Swift (`--swift`); this also seeds _Piece 3_ (import). Mockups: `mockups/drafts/2026-07-01-theme-gallery.html` (8-tab, incl. Dracula/Nord/Gruvbox/Catppuccin benchmarks) + `-derive-compare.html`. **Pending:** the warm-charcoal Bronze + derived-Midnight retune (gallery-only so far; would change 2 shipped themes).
- **Themed terminal palette (ANSI-16 infra)**, shipped 2026-07-01 (PR #31, `014a205`); **Piece 1** of a 3-piece blue-themes roadmap. `Theme` now carries an authored 16-color ANSI palette as the single source of hue, with **strict typed derivation** (`ANSISlot` / `ANSIPalette` / `ANSIRoleMap` → `Theme.fromANSI`: `accent.primary` + the 4 state colors are typed slot refs; `accent.highlight` authored; bell/focus/keybar/predictor/banner derived) and extended terminal tokens (`cursor`/`cursorText`/`selection`); a pure `TerminalPalette` DTO. Neon Midnight + Bell Bronze migrated through the factory with exact colors preserved (regression-guarded, coral `#FF6F5E` ≠ crimson `#E5455E`). App-tier `TerminalPaletteBridge` (`applyPalette` → SwiftTerm `installColors` + native fg/bg/caret/selection) finally applies theme colors to the **live** terminal, previously `terminal.bg/fg` reached only the picker preview, hooked in `TerminalScreen` + tmux panes, recoloring on theme switch. Kit Linux-tested (833 green); App macOS-CI-validated. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-01-themed-ansi-palette-infra*`. **Follow-ons:** _Piece 2_ (the two blue themes) SHIPPED, see the entry above; _Piece 3_, theme import (iTerm2/base16 → `ANSIPalette`), largely seeded by the `themes/build.py` pipeline. Mockups: `mockups/drafts/2026-07-01-theme-*.html`.
- **Cursor-placement drag gesture**, shipped 2026-06-29 (PR #27 `db6bf53` pure engines + PR #28 `7e5de1f` App layer). Locked design `docs/brainstorming-decisions.md` §"Cursor placement". Pure (Linux-tested, 32 tests): `CursorDragEngine` (gain curve + 1.5-cell vertical dead-zone + sub-cell remainder), `CursorHaloGeometry`, `CursorArrowStream`, `LoupeWindow`. App (macOS-CI-compiled): `CursorHaloView` + `CursorDragController`, faint 60pt halo at the cursor + a halo-gated single-finger pan that synthesizes arrow keys (`encodeKey(.arrow)` → `sendTerminalInput`, reading the live cursor via the confirmed-public `getTerminal().buffer.x/.y`); installed raw-PTY (single) + tmux (per-pane, focused-only); works under `mouse=a`. **App layer is compile-verified only, UNTUNED** (see Next). **Deferred slices** (uncertain SwiftTerm APIs): loupe magnifier (needs `getChar` row text), offscreen `⌖` scrollback indicator (undocumented `scrolled(position:)`), selection-handle suppression (no public selection-active signal).
- **Predictor 4c follow-ups**, shipped 2026-06-27 (PR #22, 53c198f): `flushPredictor()` flushes learned state on app-background (`scenePhase`) as well as teardown; `TerminalShellOutput` gained an `onHarvestBytes` slot so raw-shell (degraded) output trains the predictor without the render closure clobbering it; tmux harvest restricted to visible panes (early-return on dropped panes); `output.onBytes`/`onHarvestBytes` nilled in `teardown`. App-tier, macOS-CI-validated; interaction-level check rides the pending Phase 4 Simulator/device pass.
- **Predictor secret-exclusion (8-layer defense-in-depth)**, keeps typed secrets out of the predictor vocabulary; **Phases 1-4 SHIPPED**. P1 (PR #43) L1 buffer-anchored echo (`EchoOracle`/`SwiftTermEchoOracle` grid inference) + `settleLine`; P2 (#44) L3 bracketed-paste exclusion + L4 arg-position denylist + leading-space opt-out; P3 (#45) L5 credential-format/JWT/PEM patterns in `TokenFilter` + L6 frequency-graduation (`GraduationTier`: graduate iff ≥3 distinct contexts OR ≥3 nil-repeats → a once-typed password never graduates). **P4 (PR #46)** L7 non-recoverable storage: a graduating token carries `LearnConfidence` (`.high` iff echo-confirmed ∧ not opted-out ∧ not pattern-adjacent, else `.low`); low-confidence tokens store only the lossy CMS count, never the literal in the `PrefixIndex` (`storeLiteral:false` on both unigram+bigram axes) → a missed secret degrades from recoverable plaintext to an anonymous counter increment. Plus the two forget tools: **forget-last-line** (surgical, eraser on the predictor strip drops the current line's un-graduated tokens) and **panic-purge** (Settings → **Privacy** → "Forget everything the predictor learned", wipes `RollingVocabulary`+bigram+ephemeral tier + deletes `learned.sketch`, keeps the bundled `SeedStore`). Kit Linux-tested (1028 green, +20); App tier (capture-site plumbing, strip eraser, Privacy screen) macOS-CI-validated. Spec/plan: `docs/superpowers/{specs,plans}/2026-07-0{4,5}-predictor-secret-exclusion-*`; agent memory `predictor-secret-exclusion-design`. **Deferred:** L2 (mosh prediction-engine echo), L8 (sync-boundary firewall). **Live-session panic-purge RESOLVED (PR #47, `c4fbe3a`):** `AppStores` holds a `weak activePredictorSession` (`PredictorPurgeable`; a session registers on `startPredictor`, deregisters on `teardown`/incognito with a `=== self` guard), and `purgePredictorLearned()` resets that engine BEFORE deleting disk → a Settings purge over a live session no longer leaves stale learned state to re-flush.
- **Theme picker**, Settings → Appearance theme picker shipped (gear on the host list → Settings → Appearance → Theme). Neon Midnight (free) / Bell Bronze (Pro-gated). Pure catalog + Pro-gate (`ThemeID`/`ThemeDescriptor`/`Theme.catalog`/`resolveTheme`) is Linux-tested; live switching via root `\.theme` injection; Bell Bronze gated behind a **stub `ProStore`** seam (real StoreKit + Supporter badge + alt icons deferred, see Next). Spec/plan: `docs/superpowers/specs/2026-06-27-theme-picker-design.md`, `docs/superpowers/plans/2026-06-27-theme-picker.md`. Simulator pass + SwiftTerm live-recolor check pending (see Next).
- **Naming/trademark**, naming journey: **Glymr → neotilde → semicolyn**. Glymr collided with a LIVE registered GLYMR mark (2026-06-24) → forced to `neotilde`, which was ownable but disliked; a fresh round explored `fosfyre` (rename executed on the parked branch `refactor/rename-fosfyre`, never merged) before landing on **`semicolyn`**, a respell of *semicolon* (`;`, the shell command separator), owning the full namespace and trademark-clear. **Rename to semicolyn executed 2026-06-28** (this branch). `lumenhir` was the backup. SEMICOLYN mark not yet filed (pre-launch, cheapest moment to still change). Records: `docs/2026-06-28-naming-decision-semicolyn.md`; historical: `docs/2026-06-24-naming-decision-neotilde.md`, `docs/2026-06-28-naming-decision-fosfyre.md`; agent memory `naming-and-trademark`.
- **Default theme**, **Neon Midnight** (coral neon on midnight blue-black, bell-only glow); Bell-bronze retained as a switchable alternate. Spec: `docs/superpowers/specs/2026-06-25-neon-midnight-theme-design.md`.
